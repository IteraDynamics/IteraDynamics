# Argus ETH-USD instance — env template
# Copy to /etc/argus/argus-eth.env (or .env in runtime/argus) and fill in secrets.
# Use with: systemd EnvironmentFile=/etc/argus/argus-eth.env
# Do NOT overwrite existing BTC env/service; run ETH as a separate instance.

# Product (required for ETH instance)
ARGUS_PRODUCT_ID=ETH-USD

# Mode (prime | legacy)
ARGUS_MODE=prime

# Dry-run recommended for first runs
ARGUS_DRY_RUN=1

# Optional: dashboard port when running Streamlit for ETH (avoid conflict with BTC on 8501)
# streamlit run dashboard.py --server.port 8502
# ARGUS_DASHBOARD_PORT=8502

# Coinbase API (same keys can have multiple products; state is namespaced per product)
# COINBASE_API_KEY=...
# COINBASE_API_SECRET=...

# Optional heartbeats (use different URLs per instance if desired)
# ARGUS_HEARTBEAT_PING_URL=...
# ARGUS_HEARTBEAT_FAIL_URL=...
